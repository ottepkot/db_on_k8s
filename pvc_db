apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devlaba-pvc
  namespace: jenkins1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: devlaba_storage_class
